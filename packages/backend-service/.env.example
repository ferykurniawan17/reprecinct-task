# Environment variables template for backend service

# Database Configuration
DATABASE_URL=postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/reprecinct

# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration (update with your frontend URL)
CORS_ORIGIN=http://localhost:3000

# Production CORS (for AWS deployment)
# CORS_ORIGIN=https://your-frontend-domain.amplifyapp.com

# API Configuration (for Swagger docs)
API_BASE_URL=http://localhost:4000

# Production API URL (for AWS deployment)
# API_BASE_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com/prod

# Security
JWT_SECRET=your-jwt-secret-here

# AWS Configuration (optional - can be set via AWS CLI)
# AWS_REGION=us-east-1
# AWS_PROFILE=default
